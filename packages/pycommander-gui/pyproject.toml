[project]
name = "silabs-pycommander-gui"
version = "0.1.9"
description = "Python library for Silicon Labs Simplicity Commander (GUI version)"
authors = [{ name = "Silicon Labs"}]
license = "LicenseRef-MSLA"
license-files = ["LICENSE.md"]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "silabs-pycommander-core==0.1.9"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pycommander_gui"]
ignore-vcs = true

[tool.hatch.build.targets.wheel.package-data]
"pycommander_gui._archive" = ["*.zip", "*.tar.bz"]

[tool.hatch.build.targets.wheel.hooks.custom]
path = "build_hook.py"

[project.urls]
Homepage = "https://github.com/SiliconLabsSoftware/pycommander"

[project.scripts]
pycommander-gui = "pycommander_gui.cli:main"
