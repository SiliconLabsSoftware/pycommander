# Local Development

If you are working on a local build of the pycommander-cli package, copy the relevant executable archive file for your platform (`archives/Commander-cli_<platform>_<version>.<extension>`) into this directory. Do not extract the archive; the Commander executable will be automatically extracted from this directory when needed.
